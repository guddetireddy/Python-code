# Python-code
Python Code
"""
Node is defined as
self.left (the left child of the node)
self.right (the right child of the node)
self.info (the value of the node)
"""

def levelOrder_util(root, q):
    if(not root):
        return
    if(root.info not in q):
        q.append(root.info)
    if(root.left):
        q.append(root.left.info)
    if(root.right):
        q.append(root.right.info)
    levelOrder_util(root.left, q)
    levelOrder_util(root.right, q)

def levelOrder(root):
    q = []
    levelOrder_util(root, q)
    for i in q:
        print(i, end = ' ')
    
